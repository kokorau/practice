/**
 * HeroScene Types
 *
 * Grouped type definitions for useHeroScene state management.
 */

export type {
  // Custom params types (now aliased to GenericParams)
  CustomMaskShapeParams,
  CustomSurfaceParams,
  CustomBackgroundSurfaceParams,
  SectionType,
  // Mask pattern with normalized config
  MaskPatternWithNormalizedConfig,
  // Grouped state types
  PatternState,
  BackgroundState,
  MaskState,
  FilterState,
  ForegroundState,
  PresetState,
  LayerOperations,
  ImagesState,
  InkColorHelpers,
  CanvasState,
  SerializationState,
  UsecaseState,
  EditorStateRef,
  RendererActions,
  // Helper types
  TextAnchorPosition,
  AddObjectLayerOptions,
  // RightPropertyPanel props types
  RightPanelSelectionProps,
  RightPanelForegroundProps,
  RightPanelBackgroundProps,
  RightPanelMaskProps,
  RightPanelFilterProps,
} from './HeroSceneState'
